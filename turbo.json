{
  "globalEnv": ["CI"],
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".svelte-kit/**", ".vercel/**"],
      "env": ["SUPABASE_ANON_KEY", "SUPABASE_URL", "PUBLIC_ENVIRONMENT"]
    },
    "lint": {
      "env": ["SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY"]
    },
    "format:check": {},
    "format": {},
    "test:unit": {},
    "test:e2e": {
      "env": ["SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
